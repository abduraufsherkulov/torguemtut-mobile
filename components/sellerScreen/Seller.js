import React from 'react'
import SellerProducts from './SellerProducts'

function Seller({ navigation, route }) {
    return (
        <SellerProducts navigation={navigation} route={route} />
    )
}

export default Seller
