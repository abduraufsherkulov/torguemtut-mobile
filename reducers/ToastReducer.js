import { success, warning, error } from "../assets/styles/styles"

export const toastReducer = (state, action) => {
    // console.log(state);
    switch (action.type) {
        case 'success':
            
        case 'warning':
            return
        case 'error':
            return
        default:
            return
    }
}
